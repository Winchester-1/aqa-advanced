const number = 7;

// With "for"
for (let i = 1; i <= 10; i++) {
  console.log(`${number} x ${i} = ${number * i}`)
};

// With "while"
let count = 1;
while (count <= 10) {
  console.log(`${number} x ${count} = ${number * count}`);
  count++;
};